import { createApp } from 'vue'; // Import the createApp function from the Vue library
import './style.css'; // Import the CSS stylesheet for styling the application
import App from './App.vue'; // Import the main App component from the App.vue file

// Create a new Vue application instance using the App component and mount it to the HTML element with the id 'app'
createApp(App).mount('#app');
